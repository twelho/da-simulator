use petgraph::{Graph, Undirected};
use crate::{Node, PnAlgorithm};

struct BipartiteMaximalMatching;

impl PnAlgorithm for BipartiteMaximalMatching {
    fn init() {
        todo!()
    }

    fn send(n: &Node) {
        todo!()
    }

    fn receive(n: &Node) {
        todo!()
    }
}
